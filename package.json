{
  "name": "location-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "echo 'Installing dependencies...' && npm install --prefix client && npm install --prefix server && echo 'Building client...' && npm run build --prefix client && echo 'Build complete - frontend is in server/public'",
    "start": "cd server && npm start",
    "dev": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "ngeohash": "^0.6.3",
    "node": "^18.20.4",
    "socket.io": "^4.7.5"
  }
}
