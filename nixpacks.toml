[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --omit=dev --prefix client",
  "npm install --omit=dev --prefix server"
]

[phases.build]
cmds = [
  "npm run build --prefix client"
]

[start]
cmd = "cd server && npm start"
